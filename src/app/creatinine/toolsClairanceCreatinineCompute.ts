//TEST (page à supprimer si inutilisée)

import { ClairanceInput } from "./calculator";

export function toolsClairanceCreatinineCompute(data: ClairanceInput) {
    if (FormData.arguments.year !== undefined){
        console.log(FormData.arguments.year);
    }
}